---
apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetport }}
  selector:
    app: demo
  type: LoadBalancer